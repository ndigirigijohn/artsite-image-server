# ARTSITE IMAGE SERVER


To run this project:

You will need to have node js and npm installed in your machine.

Run npm install to install all the required npm packages.

Run npm start to start the application
